version: '3.8'

services:
  footprints:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_PATH=/data/footprints.db
      - PORT=3000
      - RUST_LOG=footprints=info
    restart: unless-stopped
